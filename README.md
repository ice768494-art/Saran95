# Saran Portfolio

A responsive one-page portfolio inspired by the uploaded handwritten sketch.

## Deploy on Vercel
1. Upload `index.html` to a GitHub repository.
2. Import the repository into Vercel.
3. Deploy with the default settings.

Replace the project links and email address in `index.html` with your real links.
